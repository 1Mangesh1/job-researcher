services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - UVICORN_RELOAD=false
    volumes:
      - output:/app/output
    restart: unless-stopped

volumes:
  output:
